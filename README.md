# PIM WEB API

## Requirements

- Docker >= 18.x

## Installation

Make sure you have MongoDB docker up and running.

Then run `docker-compose build --force-rm`

## Development

```bash
# Non daemon
docker-compose up
# Daemon
docker-compose up -d
```

